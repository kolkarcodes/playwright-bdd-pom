{
  "default": {
    "require": [
       "support/**/*.ts",
      "steps/**/*.ts"
    ],
    "path": ["features/**/*.feature"],
    "requireModule": ["ts-node/register"],
    "timeout": 60000,
    "publish": true,
    "format": ["progress"]
  }
}